# Qleverfile for OMA, use with https://github.com/ad-freiburg/qlever-control
#
# qlever get-data  # ~14 GB, ~850 M triples (as of 30.07.2024)
# qlever index     # ~20 min (on an AMD Ryzen 9 5900X)
# qlever start     # ~3 sec

[data]
NAME         = oma
DATABUS_URL  = https://omabrowser.org/oma/current/
GET_DATA_CMD = mkdir -p rdf-input && curl https://omabrowser.org/All/oma-rdf-turtle.tgz --output rdf-input/oma-rdf-turtle.tgz && tar -xf rdf-input/oma-rdf-turtle.tgz -C rdf-input && riot --merge --output=NT rdf-input/*.ttl.gz rdf-input/*.ttl  > rdf-input/oma.nt
DESCRIPTION  = RDF data from ${DATABUS_URL}

[index]
INPUT_FILES     = rdf-input/*
CAT_INPUT_FILES = (cat rdf-input/oma.nt)
SETTINGS_JSON   = { "ascii-prefixes-only": true, "num-triples-per-batch": 100000, "prefixes-external": [""] }
WITH_TEXT_INDEX = false

[server]
PORT               = 7013
ACCESS_TOKEN       = ${data:NAME}_9YWKzScI4tNB
MEMORY_FOR_QUERIES = 10G
CACHE_MAX_SIZE     = 5G

[runtime]
SYSTEM = docker
IMAGE  = docker.io/adfreiburg/qlever:latest

[ui]
UI_CONFIG = dbpedia
UI_PORT = 8177
